all:
  children:
    swarm_managers:
      hosts:
        unraid-manager:
          ansible_host: "{{ unraid_vm_ip }}"
          ansible_user: "{{ ssh_user }}"
          ansible_password: "{{ ssh_user_password }}"
          ansible_become_password: "{{ ssh_user_password }}"
        proxmox-ubuntu:
          ansible_host: "{{ aber_vm_ip }}"
          ansible_user: "{{ ssh_user }}"
          ansible_password: "{{ ssh_user_password }}"
          ansible_become_password: "{{ ssh_user_password }}"
        
    swarm_workers:
      hosts:
        prodesk-ubuntu:
          ansible_host: "{{ prodesk_vm_ip }}"
          ansible_user: "{{ ssh_user }}"
          ansible_password: "{{ ssh_user_password }}"
          ansible_become_password: "{{ ssh_user_password }}"
        # windows-pc:
        #   ansible_host: "{{ worker_2_ip }}"
        #   ansible_user: "{{ ssh_user }}"
        #   ansible_password: "{{ ssh_user_password }}"
        #   ansible_connection: ssh
        #   ansible_become_password: "{{ ssh_user_password }}"

